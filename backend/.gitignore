# ===================================
# BACKEND-SPECIFIC GITIGNORE
# (Project-wide rules are in root .gitignore)
# ===================================

# Build outputs
dist/
build/
*.tsbuildinfo

# Database files (catches any .db files in backend)
*.db
*.db-shm
*.db-wal

# Uploads (preserve .gitkeep)
data/uploads/*
!data/uploads/.gitkeep

# Lock files (uncomment your package manager's lock file if not using it)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Visual Studio files
*.suo
*.ntvs*
*.njsproj
*.sln
