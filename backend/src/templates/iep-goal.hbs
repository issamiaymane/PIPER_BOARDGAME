<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IEP Goal - {{student_name}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Times New Roman', Times, serif;
            font-size: 11pt;
            line-height: 1.5;
            color: #000;
            padding: 50px 60px;
            background: #fff;
        }

        .document-header {
            border-bottom: 2px solid #000;
            padding-bottom: 15px;
            margin-bottom: 25px;
        }

        .document-title {
            font-size: 18pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 5px;
        }

        .document-subtitle {
            font-size: 10pt;
            color: #555;
        }

        .info-row {
            display: flex;
            gap: 40px;
            margin-bottom: 8px;
        }

        .info-item {
            display: flex;
            gap: 8px;
        }

        .info-label {
            font-weight: bold;
            color: #333;
        }

        .info-value {
            color: #000;
        }

        .session-stats {
            background: #f5f5f5;
            border: 1px solid #ddd;
            padding: 12px 16px;
            margin: 20px 0 25px 0;
            display: flex;
            gap: 30px;
        }

        .stat {
            display: flex;
            align-items: baseline;
            gap: 6px;
        }

        .stat-label {
            font-size: 9pt;
            color: #666;
            text-transform: uppercase;
        }

        .stat-value {
            font-size: 12pt;
            font-weight: bold;
        }

        .goal-section {
            margin-bottom: 20px;
            page-break-inside: avoid;
        }

        .section-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 8px;
            border-bottom: 1px solid #ccc;
            padding-bottom: 5px;
        }

        .section-letter {
            width: 24px;
            height: 24px;
            background: #333;
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12pt;
            font-weight: bold;
        }

        .section-title {
            font-size: 11pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .section-content {
            padding: 10px 0 10px 34px;
            min-height: 40px;
        }

        .section-content p {
            margin: 0;
            white-space: pre-wrap;
            text-align: justify;
        }

        .categories {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            padding: 10px 0 10px 34px;
        }

        .category-tag {
            background: #eee;
            border: 1px solid #ccc;
            padding: 4px 12px;
            font-size: 10pt;
        }

        .document-footer {
            margin-top: 40px;
            padding-top: 15px;
            border-top: 1px solid #ccc;
            font-size: 9pt;
            color: #666;
        }

        .signature-line {
            margin-top: 30px;
            display: flex;
            justify-content: space-between;
        }

        .signature-block {
            width: 45%;
        }

        .signature-block .line {
            border-top: 1px solid #000;
            margin-top: 30px;
            padding-top: 5px;
            font-size: 10pt;
        }

        .footer-text {
            text-align: center;
            margin-top: 20px;
            font-size: 8pt;
            color: #888;
        }

        .compliance-disclaimer {
            background: #f8f8f8;
            border: 1px solid #ddd;
            padding: 10px 15px;
            margin-bottom: 20px;
            font-size: 9pt;
            font-style: italic;
            color: #555;
            text-align: center;
        }

        @media print {
            body {
                padding: 30px 40px;
            }

            .goal-section {
                page-break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <div class="document-header">
        <div class="document-title">IEP Goal Documentation</div>
        <div class="document-subtitle">Individualized Education Program - Speech-Language Therapy</div>
    </div>

    <div class="info-row">
        <div class="info-item">
            <span class="info-label">Student:</span>
            <span class="info-value">{{student_name}}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Goal Area:</span>
            <span class="info-value">{{goal_type}}</span>
        </div>
    </div>

    <div class="info-row">
        {{#if therapist_name}}
        <div class="info-item">
            <span class="info-label">Clinician:</span>
            <span class="info-value">{{therapist_name}}</span>
        </div>
        {{/if}}
        {{#if deadline}}
        <div class="info-item">
            <span class="info-label">Target Date:</span>
            <span class="info-value">{{deadline}}</span>
        </div>
        {{/if}}
    </div>

    <div class="session-stats">
        <div class="stat">
            <span class="stat-label">Target Accuracy:</span>
            <span class="stat-value">{{target_percentage}}%</span>
        </div>
        <div class="stat">
            <span class="stat-label">Sessions to Confirm:</span>
            <span class="stat-value">{{sessions_to_confirm}}</span>
        </div>
        <div class="stat">
            <span class="stat-label">Prompt Level:</span>
            <span class="stat-value">{{prompt_level}}</span>
        </div>
    </div>

    <!-- Goal Description -->
    <div class="goal-section">
        <div class="section-header">
            <div class="section-letter">G</div>
            <span class="section-title">Goal Description</span>
        </div>
        <div class="section-content">
            <p>{{goal_description}}</p>
        </div>
    </div>

    <!-- Baseline -->
    {{#if baseline}}
    <div class="goal-section">
        <div class="section-header">
            <div class="section-letter">B</div>
            <span class="section-title">Baseline</span>
        </div>
        <div class="section-content">
            <p>{{baseline}}</p>
        </div>
    </div>
    {{/if}}

    <!-- Therapy Categories -->
    <div class="goal-section">
        <div class="section-header">
            <div class="section-letter">C</div>
            <span class="section-title">Therapy Categories</span>
        </div>
        <div class="categories">
            {{#each categories}}
            <span class="category-tag">{{this}}</span>
            {{/each}}
        </div>
    </div>

    <!-- Notes -->
    {{#if comments}}
    <div class="goal-section">
        <div class="section-header">
            <div class="section-letter">N</div>
            <span class="section-title">Notes</span>
        </div>
        <div class="section-content">
            <p>{{comments}}</p>
        </div>
    </div>
    {{/if}}

    <div class="document-footer">
        <div class="compliance-disclaimer">
            AI-assisted documentation reviewed, approved, and finalized by a licensed Speech-Language Pathologist.
        </div>
        <div class="signature-line">
            <div class="signature-block">
                <div class="line">Clinician Signature / Date</div>
            </div>
            <div class="signature-block">
                <div class="line">Supervisor Signature / Date (if applicable)</div>
            </div>
        </div>
        <div class="footer-text">
            Generated by PIPER - Personalized IEP Progress & Evaluation Reporter | {{generated_date}}
        </div>
    </div>
</body>
</html>
