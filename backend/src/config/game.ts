/**
 * Game Configuration
 * Centralized configuration for gameplay values
 */

export const gameConfig = {
  // Scoring
  scoring: {
    correctAnswer: 10,
    incorrectAnswer: 0,
  },

  // Session defaults (pagination limits)
  sessionDefaults: {
    historyLimit: 20,
    therapistHistoryLimit: 50,
  },
};
