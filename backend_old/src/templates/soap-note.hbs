<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SOAP Note - {{student_name}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Times New Roman', Times, serif;
            font-size: 11pt;
            line-height: 1.5;
            color: #000;
            padding: 50px 60px;
            background: #fff;
        }

        /* Header */
        .document-header {
            border-bottom: 2px solid #000;
            padding-bottom: 15px;
            margin-bottom: 25px;
        }

        .document-title {
            font-size: 18pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 5px;
        }

        .document-subtitle {
            font-size: 10pt;
            color: #555;
        }

        /* Patient Info Table */
        .patient-info {
            width: 100%;
            margin-bottom: 25px;
            border-collapse: collapse;
        }

        .patient-info td {
            padding: 6px 0;
            vertical-align: top;
        }

        .patient-info .label {
            font-weight: bold;
            width: 120px;
            color: #333;
        }

        .patient-info .value {
            color: #000;
        }

        .info-row {
            display: flex;
            gap: 40px;
            margin-bottom: 8px;
        }

        .info-item {
            display: flex;
            gap: 8px;
        }

        .info-label {
            font-weight: bold;
            color: #333;
        }

        .info-value {
            color: #000;
        }

        /* Session Stats */
        .session-stats {
            background: #f5f5f5;
            border: 1px solid #ddd;
            padding: 12px 16px;
            margin-bottom: 25px;
            display: flex;
            gap: 30px;
        }

        .stat {
            display: flex;
            align-items: baseline;
            gap: 6px;
        }

        .stat-label {
            font-size: 9pt;
            color: #666;
            text-transform: uppercase;
        }

        .stat-value {
            font-size: 12pt;
            font-weight: bold;
        }

        /* SOAP Sections */
        .soap-section {
            margin-bottom: 20px;
            page-break-inside: avoid;
        }

        .section-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 8px;
            border-bottom: 1px solid #ccc;
            padding-bottom: 5px;
        }

        .section-letter {
            width: 24px;
            height: 24px;
            background: #333;
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12pt;
            font-weight: bold;
        }

        .section-title {
            font-size: 11pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .section-content {
            padding: 10px 0 10px 34px;
            min-height: 40px;
        }

        .section-content p {
            margin: 0;
            white-space: pre-wrap;
            text-align: justify;
        }

        /* Footer */
        .document-footer {
            margin-top: 40px;
            padding-top: 15px;
            border-top: 1px solid #ccc;
            font-size: 9pt;
            color: #666;
        }

        .signature-line {
            margin-top: 30px;
            display: flex;
            justify-content: space-between;
        }

        .signature-block {
            width: 45%;
        }

        .signature-block .line {
            border-top: 1px solid #000;
            margin-top: 30px;
            padding-top: 5px;
            font-size: 10pt;
        }

        .footer-text {
            text-align: center;
            margin-top: 20px;
            font-size: 8pt;
            color: #888;
        }

        .compliance-disclaimer {
            background: #f8f8f8;
            border: 1px solid #ddd;
            padding: 10px 15px;
            margin-bottom: 20px;
            font-size: 9pt;
            font-style: italic;
            color: #555;
            text-align: center;
        }

        @media print {
            body {
                padding: 30px 40px;
            }

            .soap-section {
                page-break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <div class="document-header">
        <div class="document-title">SOAP Note</div>
        <div class="document-subtitle">Speech-Language Therapy Session Documentation</div>
    </div>

    <div class="info-row">
        <div class="info-item">
            <span class="info-label">Patient:</span>
            <span class="info-value">{{student_name}}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Date of Service:</span>
            <span class="info-value">{{session_date}}</span>
        </div>
    </div>

    <div class="info-row">
        {{#if therapist_name}}
        <div class="info-item">
            <span class="info-label">Clinician:</span>
            <span class="info-value">{{therapist_name}}</span>
        </div>
        {{/if}}
        {{#if goal_type}}
        <div class="info-item">
            <span class="info-label">Treatment Area:</span>
            <span class="info-value">{{goal_type}}</span>
        </div>
        {{/if}}
    </div>

    {{#if show_stats}}
    <div class="session-stats">
        <div class="stat">
            <span class="stat-label">Accuracy:</span>
            <span class="stat-value">{{accuracy}}%</span>
        </div>
        <div class="stat">
            <span class="stat-label">Trials:</span>
            <span class="stat-value">{{correct_trials}}/{{total_trials}}</span>
        </div>
        <div class="stat">
            <span class="stat-label">Prompt Level:</span>
            <span class="stat-value">{{prompt_level}}</span>
        </div>
    </div>
    {{/if}}

    <!-- Subjective -->
    <div class="soap-section">
        <div class="section-header">
            <div class="section-letter">S</div>
            <span class="section-title">Subjective</span>
        </div>
        <div class="section-content">
            <p>{{soap_subjective}}</p>
        </div>
    </div>

    <!-- Objective -->
    <div class="soap-section">
        <div class="section-header">
            <div class="section-letter">O</div>
            <span class="section-title">Objective</span>
        </div>
        <div class="section-content">
            <p>{{soap_objective}}</p>
        </div>
    </div>

    <!-- Assessment -->
    <div class="soap-section">
        <div class="section-header">
            <div class="section-letter">A</div>
            <span class="section-title">Assessment</span>
        </div>
        <div class="section-content">
            <p>{{soap_assessment}}</p>
        </div>
    </div>

    <!-- Plan -->
    <div class="soap-section">
        <div class="section-header">
            <div class="section-letter">P</div>
            <span class="section-title">Plan</span>
        </div>
        <div class="section-content">
            <p>{{soap_plan}}</p>
        </div>
    </div>

    <div class="document-footer">
        <div class="compliance-disclaimer">
            AI-assisted documentation reviewed, approved, and finalized by a licensed Speech-Language Pathologist.
        </div>
        <div class="signature-line">
            <div class="signature-block">
                <div class="line">Clinician Signature / Date</div>
            </div>
            <div class="signature-block">
                <div class="line">Supervisor Signature / Date (if applicable)</div>
            </div>
        </div>
        <div class="footer-text">
            Generated by PIPER - Personalized IEP Progress & Evaluation Reporter | {{generated_date}}
        </div>
    </div>
</body>
</html>
