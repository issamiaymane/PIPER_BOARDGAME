# ===================================
# FRONTEND-SPECIFIC GITIGNORE
# (Project-wide rules are in root .gitignore)
# ===================================

# Build outputs
dist/
build/
.vite/

# TypeScript
*.tsbuildinfo

# Lock files (uncomment your package manager's lock file if not using it)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Visual Studio files
*.suo
*.ntvs*
*.njsproj
*.sln
.env
